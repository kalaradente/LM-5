.venv/
__pycache__/
*.pyc
.DS_Store
# Vendored clone of github.com/jewbetcha/openflight — has its own git history;
# the setup wizard clones it. See README.md.
openflight_upstream/
# Machine-specific hardware IDs written by scripts/setup_wizard.sh
hardware.env
# Per-shot raw capture archives (radar + audio), written at runtime
captures/
# Local Claude Code tooling (skills, launch configs) — dev-machine only
.claude/
# Design/product briefs generated by the impeccable skill — prose, no
# actionable code; deliberately unpublished (Johnny's call, 2026-07-06)
DESIGN.md
PRODUCT.md
